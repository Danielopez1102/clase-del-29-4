alerta ('Esta funcionando');

// 1

var numero_que_queremos_evaluar = 100;

function log10(numero) {
 return math.log10(numero);
}
consola. log(log10(numero_que_queremos_evaluar));

// 2

 var text = 'mañana vamos a comer un hamburguesa, y luego  vamos al cine'

function busqueda_del_cine(texto) {
 if (texto. incluye('cine')) {
 if (texto. includes('no')) {
 consola. log('no vamos a ir al cine');
      }else{
 consola. log('vamos a ir al cine');
      }  
    }
}

busqueda_del_cine (texto); 